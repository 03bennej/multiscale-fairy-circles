from setuptools import setup, find_packages

setup(name='src',       # name of package
      version='0.0',    # version of package
      packages=find_packages())     # looks for all __init__.py files in directory